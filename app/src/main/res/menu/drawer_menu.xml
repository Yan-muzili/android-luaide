<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/files"
            android:icon="@drawable/ic_folder_collapsed"
             />
        <item
            android:id="@+id/paring"
            android:icon="@drawable/ic_paring"
             />
        <item
            android:id="@+id/toolbox"
            android:icon="@drawable/toolbox"
            />
        <item
            android:id="@+id/set"
            android:icon="@drawable/ic_set"
            />
    </group>
</menu>
